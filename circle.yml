machine:
  java:
    version: openjdk7

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  pre:
    - npm install
    - python ./vendors/closure-linter/setup.py install

  override:
    - npm run closure-lint

  post:
    - npm run build:closure-compiler
    - npm run build
