machine:
  java:
    version: openjdk7

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - npm install
  pre:
    - grunt shell:compileClosureCompiler
  post:
    - grunt build
